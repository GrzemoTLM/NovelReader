# ── OS / system ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
*.iml
*.ipr
*.iws
.vscode/

# Eclipse / STS / NetBeans (gdyby ktoś użył)
.classpath
.project
.settings/
.springBeans
.sts4-cache
.apt_generated
.factorypath
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

# ── Java / Kotlin / build artefacts ───────────────────────────────────────────
*.class
*.log
*.javadoc
*.zip
*.tar
*.tar.gz

# Gradle
.gradle/
**/.gradle/
build/
**/build/
!gradle/wrapper/gradle-wrapper.jar
.kotlinc/
.kotlin/
out/
**/out/
bin/
**/bin/

# Maven (jeśli gdzieś użyjesz)
target/

# ── Spring Boot config (lokalne warianty) ─────────────────────────────────────
application-*.yml
application-*.yaml
application-*.properties

# ── Env / sekrety ─────────────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Docker / Postgres bind mounts ─────────────────────────────────────────────
# Dane kontenera Postgresa i pgAdmina (z docker-compose)
data/postgres/
data/pgadmin/

# Kopie zapasowe bazy (trzymaj tylko .gitkeep/README)
backups/*
!backups/.gitkeep
# (jeśli wolisz trzymać README.md)
# !backups/README.md

# ── Inne pliki tymczasowe / merdżowe ──────────────────────────────────────────
*.orig
*.rej

# (opcjonalnie) plik pomocniczy ze Spring Initializr
HELP.md

# tymczasowy lokalny folder z książkami
/books/
